# —— 浏览器可读（VITE_ 会打进前端，仅用于 CDN 读已发布内容）——
VITE_SANITY_PROJECT_ID=
VITE_SANITY_DATASET=production
VITE_SANITY_API_VERSION=2024-01-01
VITE_SANITY_USE_CDN=true

# Vercel / 生产域名部署后：务必在 https://sanity.io/manage → 本项目 → API → CORS origins
# 中加入 https://你的站点.vercel.app 与正式域名，否则浏览器请求 Sanity API 会 403。

# 可选：只读 token（一般公开站不需要）
# VITE_SANITY_READ_TOKEN=

# —— 仅服务端 / 构建环境（切勿使用 VITE_ 前缀；勿提交含真实 token 的 .env）——
# 询盘 API + 翻译 API（Vite 开发中间件 + Vercel Serverless）写入 Sanity 时使用：
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_VERSION=2024-01-01
# 在 https://sanity.io/manage 创建具有 Editor 权限的 token
# 严禁为写入 token 加 VITE_ 前缀（否则会打进前端包，造成泄露）
SANITY_API_WRITE_TOKEN=

# —— 翻译服务（Vercel Serverless /api/webhook/translate）——
# DeepSeek API 密钥（必填，用于 ZH→多语言翻译）
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-chat

# Studio 手动「同步翻译」按钮绕过 Sanity 签名验证的共享密钥（可选；与 studio/.env.local 中 SANITY_STUDIO_TRANSLATE_BYPASS_KEY 值相同）
SANITY_STUDIO_TRANSLATE_BYPASS_KEY=

# Sanity 控制台 Webhook 签名密钥（可选；不填则跳过 HMAC 校验）
SANITY_WEBHOOK_SECRET=
