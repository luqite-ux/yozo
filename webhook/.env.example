# 翻译 Webhook（本地 / 部署）
# —— 推荐：不复制本文件也可启动 ——
# server.js 会自动合并（仅填充当前环境中仍为空的变量）：
#   1) webhook/.env
#   2) ../studio/.env.local   （DEEPSEEK_*、SANITY_STUDIO_*）
#   3) ../website/.env.local  （SANITY_API_WRITE_TOKEN、VITE_SANITY_*）
#   4) ../website/.env
# 并自动把 VITE_SANITY_PROJECT_ID / SANITY_STUDIO_PROJECT_ID 填到 SANITY_PROJECT_ID（若后者为空）。

# 以下仅在「无 website/studio 本地文件」或「部署环境」时需要填写：

# SANITY_PROJECT_ID=
# SANITY_DATASET=production
# SANITY_WRITE_TOKEN=
# 或
# SANITY_API_WRITE_TOKEN=

# DeepSeek（若 studio/.env.local 已配置则可省略）
# DEEPSEEK_API_KEY=
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# DEEPSEEK_MODEL=deepseek-chat

# Sanity 控制台 Webhook 的签名密钥（可选；不设则不做 HMAC 校验）
# SANITY_WEBHOOK_SECRET=

# 若已设置 SANITY_WEBHOOK_SECRET：Studio 文档菜单「同步翻译」需在 webhook 与 studio 配置相同：
# SANITY_STUDIO_TRANSLATE_BYPASS_KEY=随机长字符串

# PORT=3001
# DEBUG_WEBHOOK_ENV=1
#
# HTTP 入口（供 Studio / Sanity Webhook 调用）：
#   POST http://<host>:<PORT>/webhook/translate
# Studio / Vercel 环境变量：SANITY_STUDIO_TRANSLATION_WEBHOOK_URL 须指向上面的完整 URL（含 /webhook/translate）。
#
# 浏览器从 Studio 跨域 POST 时须通过 CORS。默认放行 localhost:3333 / 127.0.0.1:3333 等；
# 若用局域网 IP 打开 Studio（如 http://192.168.1.5:3333），请增加：
# SANITY_STUDIO_TRANSLATE_CORS_ORIGINS=http://192.168.1.5:3333
# 部署在 https://*.vercel.app 的 Studio 默认放行（可用 TRANSLATE_CORS_ALLOW_VERCEL_STUDIO=0 关闭）。
